import glamorous from 'glamorous'
import v from "../../../styles/variables";

export const TextFieldSpace = glamorous.div({
  background: v.colorBlack5,
  boxShadow: `0px 0px 10px 3px ${v.colorBlack50}`,
  padding: '2em',
  borderRadius: '10px'
});